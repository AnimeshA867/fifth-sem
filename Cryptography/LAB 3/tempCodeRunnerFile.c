 if (!inverse)
    {
        printf("No multiplicative inverse exists\n");
    }
    else
    {

        printf("The multiplicative inverse of %d over modulo %d is %d \n", x, n, multiplicativeInverse(x, n));
    }